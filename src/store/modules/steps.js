import { isContext } from "vm";

const state = {
    step: 0
}

const getters = {
    getStep : state => state.step
}

const actions = {

}

const mutations = {

}

export default {
    state,
    getters,
    actions,
    mutations
}